# kabukuri-manuals リポジトリ

## Why（目的）
カブクリの現場マニュアルを管理し、GitHub Pages（Jekyll）で公開する。閲覧者にはZOEN-TECHメンバー外（提携医師・配送業者）が含まれるため、**publicで維持する**（内部情報を含むダッシュボード等は別運用）。

## Structure（構成）
```
docs/
├── index.md            ← ホーム
├── _config.yml         ← Jekyll設定（url/baseurl 未設定）
├── field/
│   ├── doctor/         ← 医師向けマニュアル
│   └── delivery/       ← 配送担当向けマニュアル
└── executive/          ← 【未決事項】経営向け文書の置き場。publicリポと矛盾しうるため、掲載可否の基準はユーザーが決めるまで何も置かない
```

## Rules
- 必ず日本語で記述すること
- **publicリポジトリ**である。患者情報・売上等の内部数値・認証情報・内部URLは絶対に載せない
- 患者の例示は**完全な架空データのみ**（架空の氏名イニシャル・住所・日付・注文番号。実在患者由来の情報は加工しても使わない）
- 編集はローカルで進めてよいが、**push・デプロイ・関係者周知は「変更内容を提示 → ユーザー承認」後のみ**
- プレースホルダー（「内容を追加してください」等）が残るページを公開完了扱いしない
- 業務フロー自体の変更を伴う場合は `repo_kabukuri/docs/decisions/`（**別リポジトリ**）にも記録する

## Workflow
1. 変更内容をユーザーに提示して承認を得る
2. `docs/` 配下を編集
3. push → GitHub Pagesに反映
4. 反映後のURLを提示し、必要なら関係者（医師・配送）への周知をユーザーに促す

### ⚠️ 移行中の現状（2026-07-02時点）
- リポジトリ移行作業中: デフォルトブランチは `claude/separate-manual-repo-KXMm0` で **mainブランチは未作成**
- **GitHub Pagesは未設定**（公開URLはまだ存在しない）。上記Workflow 3-4 はPages設定完了後に機能する
- 公開設定（mainブランチ作成・Pages有効化・URL確定）が済んだらこのセクションを削除し、Structure/Workflowを実URLで更新すること
